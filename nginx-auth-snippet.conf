# Production-only nginx snippet. Mounted into the container by the
# Ansible-rendered compose.override.yml. Not used in local dev.
auth_basic           "Restricted";
auth_basic_user_file /etc/nginx/.htpasswd;