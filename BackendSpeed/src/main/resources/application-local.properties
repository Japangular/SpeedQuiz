# PostgreSQL
spring.datasource.postgresql.url=jdbc:postgresql://localhost:5433/speedquizdb

# Flyway
spring.flyway.url=jdbc:postgresql://localhost:5433/speedquizdb

# SQLite (read-only Anki data)
spring.datasource.sqlite.url=jdbc:sqlite:./anki/collection.db
spring.datasource.sqlite.driverClassName=org.sqlite.JDBC

# App
app.deck-import.path=./exampleHtmls