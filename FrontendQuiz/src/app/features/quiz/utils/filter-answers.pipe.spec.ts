import { FilterAnswersPipe } from './filter-answers.pipe';

describe('FilterAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
